/**
 * This package contains the event handler classes for the GUI.<p>
 * All the views has its own class that sends read/write events to 
 * the database and receives events from the other GUI components.
 * In addition every view has another class that handles events 
 * that is passed to the views, such as events that notifies the
 * view that updated data is available from the database.
 */
package pizzaProgram.gui.EventHandlers;